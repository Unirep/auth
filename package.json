{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "git submodule init && git submodule update",
    "lint": "prettier .",
    "build:circuits": "node scripts/buildParallel.mjs",
    "build:contracts": "hardhat compile && node scripts/buildVerifiers.mjs",
    "build": "yarn build:circuits && yarn build:contracts",
    "test": "hardhat test ./test/contracts/*.test.js"
  },
  "dependencies": {
    "poseidon-solidity": "^0.0.5"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "5",
    "global-factory": "^0.0.2",
    "hardhat": "2.12.3",
    "poseidon-lite": "^0.2.0",
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3",
    "randomf": "^0.0.3",
    "snarkjs": "^0.6.11"
  }
}
